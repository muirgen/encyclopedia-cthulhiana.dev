{% extends "EncyclopediaAdminBundle::layout.html.twig" %}

{% block body %}
<h3><a href="{{ path('_admin_persons') }}">Person</a> > Edit : <a href="{{ path('_admin_persons_edit', {'id' : person.id}) }}">{{ person.name }}</a> > Alias : {{ entity.name }}</h3>
<div class="container_12 edit">
    <div class="grid_6 block">
        <fieldset><legend>Edit</legend>    
        {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}

        {{ form_row(edit_form.name) }}
        {{ form_row(edit_form.id_person) }}

        {{ form_end(edit_form) }}
        </fieldset>
    </div>
</div>
{% endblock %}
